# ============================================================
# All environment variables live in the ROOT .env file.
# ============================================================
# This file is intentionally empty — do NOT add variables here.
#
# Setup (from project root):
#   cp .env.example .env
#
# The backend reads the root .env automatically via pathlib.
# See /.env.example for the complete variable reference.
# ============================================================
